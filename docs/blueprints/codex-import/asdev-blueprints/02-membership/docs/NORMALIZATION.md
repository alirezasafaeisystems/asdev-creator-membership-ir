# Normalizationهای حیاتی — Membership

این‌ها از روز اول پشتیبانی و ریسک امنیتی شما را کم می‌کنند:

1) Normalize Creator Slug (`src/lib/normalizeSlug.ts`)
2) Normalize Email/Phone (`src/lib/normalizeIdentity.ts`)
3) Normalize returnUrl (ضد Open Redirect) (`src/lib/normalizeReturnUrl.ts`)

