# چک‌لیست‌های اجرایی (گام‌محور)

## Audit — ۱۴ گام
1) Repo scaffold (Next.js + Prisma + Postgres)
2) Schema v1 (Audit models) + migration
3) Create Run API + token generation
4) Worker skeleton + enqueue AUDIT_RUN
5) Fetch HTML + headers + error logging
6) Extract resources (script/link/img)
7) Rule set v1 (10 rules) + findings
8) Summary JSON v1 ذخیره در DB
9) Report UI v1 (Top fixes + findings)
10) Share UX (copy link + OG)
11) Lead capture (email/phone)
12) Orders + callback (حتی MOCK)
13) PDF export v1
14) SEO baseline (sample report + guides + sitemap)

## Membership — ۱۴ گام
1) تعیین لانچ اسکوپ MVP (features freeze)
2) Landing/Positioning + CTA
3) Pricing تصمیم
4) Onboarding wizard
5) Public creator page v1
6) Checkout flow v1
7) Callback idempotency (WebhookReceipt unique)
8) Amount mismatch guard
9) Subscription activation transactional
10) Content upload + publish
11) Tokenized download + entitlement guard
12) Dashboard v1 (plans/content/members)
13) Admin ops v1 (payments + reconcile)
14) Worker jobs: reconcile/expire/cleanup + ops summary

